{
  "variables": {
    "source_image": "1b5482ef-eb06-47d7-bc21-4c51027d7e99",
    "source_image_name": "KPI_Scn4_host_image",
    "flavor": "s2.large.2",
    "region": "eu-de",
    "ssh_user": "linux",
    "network_id": "ac462305-619a-4726-81e7-43b56a45cba4",
    "sec_grp_id": "d298a575-6321-450a-b91e-353b3224215d",
    "influx_url": "{{env `INFLUXDB_URL`}}"
  },
  "builders": [
    {
      "type": "openstack",
      "ssh_username": "{{user `ssh_user` }}",
      "image_name": "{{user `source_image_name` }}",
      "source_image": "{{user `source_image` }}",
      "flavor": "{{user `flavor` }}",
      "region": "{{user `region` }}",
      "networks": ["{{user `network_id`}}"],
      "security_groups": ["{{user `sec_grp_id`}}"],
      "availability_zone": "eu-de-01",
      "floating_ip_network": "admin_external_net",
      "use_blockstorage_volume": true
    }
  ],
  "provisioners": [
    {
      "environment_vars": [
        "INFLUXDB_URL={{user `influx_url`}}"
      ],
      "script": "scenario4/scripts/provision.sh",
      "type": "shell"
    }
  ]
}