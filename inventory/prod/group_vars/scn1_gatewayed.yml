---
# Special SSH for accessing hosts behing Bastion/Jump

ansible_ssh_common_args: '-o ProxyCommand="ssh -W %h:%p -q -i {{ lookup("env", "RSA_PRIVATE_KEY") }} linux@{{ lookup("env", "BASTION_PUBLIC_IP") }}"'